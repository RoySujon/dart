void main() {
  var mylist = List<dynamic>.filled(3, 0); //length, fill
  mylist[0] = 'rain';
  mylist[1] = 'Rifat';
  mylist[2] = 'Ripon';
  print(mylist);
  // print(mylist[2]);
  // for (dynamic roy in mylist) {
  //   print(roy);
  // }

  // for (var i = 0; i < mylist.length; i++) {
  //   print(mylist[i]);
  // }

  // mylist.forEach((element) => print(element));
}
